shell -$SHELL

altscreen on
vbell off
startup_message off

escape ^|^|

bind 'L' next
bind 'H' prev

hardstatus alwayslastline
hardstatus string '%{= kG}[ %{G}%H %{g}][%= %{= kw}%?%-Lw%?%{r}(%{W}%n*%f%t%?(%u)%?%{r})%{w}%?%+Lw%?%?%= %{g}][%{B} %m-%d %{W}%c %{g}]'

maptimeout 0
